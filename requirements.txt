numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
h5py>=3.8
tables>=3.8
torch>=2.0
geometric_kernels>=0.3
requests>=2.28

# Optional: only for run_deep_ensemble_gnn.py (the Deep Ensemble GNN baseline,
# Appendix E). Not needed for reproduce.py or any other runner.
# torch_geometric>=2.3

# Optional: only for run_bktr_baseline.py (the cited BKTR method, Lei, Labbé & Sun,
# Bayesian Analysis 2025).
# The main leaderboard's bktr column is an SE-kernel spatial-GP baseline computed
# inside run_all_methods.py (see README); pyBKTR is not needed to reproduce it.
# pyBKTR>=0.2
